This is a custom layout for onboard, an open source virtual keyboard for Linux.

Install onboard with

$ sudo apt-get install onboard

and then put these files in $HOME/.local/share/onboard/layouts.

